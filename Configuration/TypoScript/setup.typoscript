plugin.tx_buildinfo  {
    settings {
        buildNumberFile = {$plugin.tx_buildinfo.settings.buildNumberFile}
        buildTimestampFile = {$plugin.tx_buildinfo.settings.buildTimestampFile}
    }
}
